{%- use 'io.twig' -%}
{%- use 'constructor.twig' -%}

export class {{ class.name }}{% if class.parent %} extends {{ class.parent.name }}{% else %} extends DofusMessage{% endif %} {
    public static id{% if not class.parent %}: number{% endif %} = {{ class.id }};
{%~ block class_fields -%}
    {{ parent()|ident(4) }}
{%- endblock -%}
{%- block constructor -%}
    {{ parent()|ident(4) }}
{%~ endblock -%}
{%~ block serialize -%}
    {{ parent()|ident(4) }}
{%~ endblock -%}
{%~ block deserialize -%}
    {{ parent()|ident(4) }}
{%- endblock %}
}
